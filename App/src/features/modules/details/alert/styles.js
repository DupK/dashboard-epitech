import {
	StyleSheet
} from "react-native";

const styles = StyleSheet.create({

	container: {
		flex: 1,
		backgroundColor: '#F6C33A',
		justifyContent: 'center',
		alignItems: 'center',
	},

	text: {
		color: '#F9F9F9',
		fontSize: 12,
		fontWeight: 'bold',
	}

});

export default styles;
