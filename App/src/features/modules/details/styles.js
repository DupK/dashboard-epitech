import {
	StyleSheet
} from 'react-native';

const styles = StyleSheet.create({

	container: {
		flex: 1,
		backgroundColor: '#263443',
	},

	alertContainer: {
		flex: 0.05,
	},

	bannerContainer: {
		flex: 0.5,
	},

	sectionsContainer: {
		flex: 0.45,
	}

});

export default styles;